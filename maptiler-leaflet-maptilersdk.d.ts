declare module '@maptiler/leaflet-maptilersdk' {
  // Add specific types if known, or use 'any' as a placeholder
  export class MaptilerLayer {
    constructor(options: any)
    addTo(map: any): void
  }
}
